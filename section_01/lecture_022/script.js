const has_drivers_licence = true;
const has_good_vision = false;

//                             and
console.log(has_drivers_licence && has_good_vision);

//                              or
console.log(has_drivers_licence || has_good_vision);

//         not
console.log(!has_drivers_licence);