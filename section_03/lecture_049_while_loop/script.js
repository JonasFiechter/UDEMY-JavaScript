const jonas_array = [
    'Jonas',
    'Fiechter',
    2037 - 1993,
    ['Michael', 'Peter', 'Steven'],
];

const types = [];

let i = 0;
while (i < 10) {
    console.log(i);
    i++
}

let dice = Math.trunc(Math.random() * 6) + 1;

while (dice !== 6) {
    console.log(dice);
    dice = Math.trunc(Math.random() * 6) + 1;
}